# Supertrainapp Backend

Бекенд-сервер для додатку управління тренуваннями на базі **Node.js** з **Express** та **Prisma ORM**.

## 📋 Таблиця вмісту

- [Стек технологій](#-стек-технологій)
- [Вимоги](#-вимоги)
- [Встановлення](#-встановлення)
- [Конфігурація](#-конфігурація)
- [Запуск проекту](#-запуск-проекту)
- [Структура проекту](#-структура-проекту)
- [База даних](#-база-даних)
- [API Ендпоінти](#-api-ендпоінти)
- [Розробка](#-розробка)

## 🛠 Стек технологій

- **Node.js** - JavaScript runtime
- **Express** - Веб-фреймворк
- **Prisma** - ORM для роботи з БД
- **SQLite** - База даних
- **Multer** - Обробка файлів (зображення, відео)
- **Sharp** - Обробка зображень
- **CORS** - Кросс-доменні запити
- **Nodemon** - Автоперезавантаження під час розробки

## 📦 Вимоги

- Node.js >= 18.x
- npm або yarn

## 🚀 Встановлення

### 1. Встановіть залежності

```bash
npm install
```

### 2. Налаштуйте змінні середовища

Створіть файл `.env` в корні папки `server`:

```env
DATABASE_URL="file:./dev.db"
```

### 3. Ініціалізуйте базу даних

```bash
npx prisma migrate dev
```

Цей набір команди:
- Застосує всі міграції
- Запустить seed скрипт для додання вихідних даних
- Сгенерує Prisma Client

## ⚙️ Конфігурація

### CORS

За замовчуванням сервер приймає запити від:
- `http://localhost:5173` (фронтенд в режимі розробки)

Змініть `corsOptions` в `server.js` для інших доменів.

### Порт

Сервер запускається на портах:
- **3000** (звичайно)
- **5000** (як резервний)

## 🏃 Запуск проекту

### Режим розробки з автоперезавантаженням

```bash
npm run dev
```

### Режим production

```bash
npm start
```

## 📁 Структура проекту

```
server/
├── routes/                      # API маршрути
│   ├── ExerciseRoutes.js       # Маршрути для вправ
│   └── BaseData.js             # Маршрути для базових даних
├── controller/                  # Контролери (на даний момент пусто)
├── database/                    # Конфігурація БД (на даний момент пусто)
├── prisma/
│   ├── schema.prisma           # Схема бази даних
│   ├── seed.js                 # Скрипт для інічалізації БД
│   └── migrations/             # Історія міграцій
├── uploads/                     # Папка для завантажених файлів
├── examples/                    # Приклади запитів
├── server.js                    # Точка входу в додаток
├── prismaInit.js               # Інічалізація Prisma Client
├── uploadMedia.js              # Утиліти для обробки медіа
├── package.json                # Залежності проекту
└── .env                        # Змінні середовища (не в git)
```

## 🗄️ База даних

### Моделі даних

#### Exercise (Вправа)
- `id` - Унікальний ідентифікатор
- `title` - Назва вправи
- `description` - Опис
- `images` - Зв'язана таблиця зображень
- `videos` - Зв'язана таблиця відео
- `muscles` - Позначені групи м'язів
- `routines` - Рутини, які містять цю вправу
- `createdAt`, `updatedAt` - Мітки часу

#### ExerciseImage & ExerciseVideo
- Зберігають шляхи до медіа-файлів
- Автоматично видаляються при видаленні вправи

#### Routine (План тренування)
- `id` - Унікальний ідентифікатор
- `title` - Назва плану
- `description` - Опис
- `categories` - Категорії (Many-to-Many)
- `exercises` - Вправи в плані (з порядком і параметрами)

#### RoutineCategory (Категорія)
- `id` - Унікальний ідентифікатор
- `nameEn` - Назва англійською
- `nameUa` - Назва українською
- `description` - Опис
- `icon` - Emoji або назва іконки
- `color` - Hex колір для UI

#### Muscle (М'яз)
- `id` - Унікальний ідентифікатор
- `name` - Назва м'яза
- `description` - Опис

#### Workout (Тренування)
- Реєстрація проведених тренувань
- Зв'язок з рутиною
- Інформація про виконані вправи

### Міграція БД

Для створення нової міграції після зміни `schema.prisma`:

```bash
npx prisma migrate dev --name <назва_міграції>
```

Приклад:
```bash
npx prisma migrate dev --name add_routine_categories
```

Для повного скидання БД (розробка):
```bash
npx prisma migrate reset
```

### Seed дані

Скрипт `prisma/seed.js` автоматично додає:
- Категорії рутин
- Базові групи м'язів
- Приклади вправ

Запустити вручну:
```bash
npx prisma db seed
```

## 🔌 API Ендпоінти

### Вправи

#### Отримати всі вправи
```
GET /api/exercise
```

#### Отримати вправу за ID
```
GET /api/exercise/:id
```

#### Створити вправу
```
POST /api/exercise/create
```
**Тіло запиту:**
```json
{
  "title": "Віджимання",
  "description": "Класична вправа на грудь",
  "muscleIds": [1, 2]
}
```

#### Оновити вправу
```
PUT /api/exercise/:id
```

#### Видалити вправу
```
DELETE /api/exercise/:id
```

#### Завантажити зображення вправи
```
POST /api/exercise/:id/upload-image
```
**Форма:** multipart/form-data з полем `image`

#### Завантажити відео вправи
```
POST /api/exercise/:id/upload-video
```
**Форма:** multipart/form-data з полем `video`

### Базові дані

#### Отримати всі категорії
```
GET /api/baseData/categories
```

#### Отримати всі групи м'язів
```
GET /api/baseData/muscles
```

#### Отримати всі рутини
```
GET /api/baseData/routines
```

### Рутини

#### Створити рутину
```
POST /api/routine/create
```
**Тіло запиту:**
```json
{
  "title": "Тиск лежачи",
  "description": "План для грудей і трицепса",
  "categoryIds": [1, 2],
  "routineExercises": [
    {
      "exerciseId": 5,
      "reps": 8,
      "sets": 4,
      "rest": 90
    }
  ]
}
```

## 🔧 Розробка

### Перегляд бази даних

```bash
npx prisma studio
```

Відкриває веб-інтерфейс для управління БД на `http://localhost:5555`

### Генерування Prisma Client

```bash
npx prisma generate
```

Запускається автоматично при встановленні залежностей.

### Перевірка схеми

```bash
npx prisma validate
```

### Здійснення наслідки

```bash
npx prisma migrate status
```

## 🐛 Розв'язання проблем

### Помилка: "Prisma Client не сгенерований"
```bash
npm install
npx prisma generate
```

### Помилка: "БД заблокована"
Закрийте Prisma Studio та переспробуйте.

### Помилка CORS
Переконайтесь, що URL фронтенду доданий в `corsOptions` в `server.js`.

### Помилка міграції
```bash
npx prisma migrate resolve --rolled-back <migration_name>
# або повний скидання
npx prisma migrate reset
```

## 📝 Записи на майбутнє

- [ ] Реалізувати контролери в папці `controller/`
- [ ] Додати валідацію вхідних даних
- [ ] Реалізувати аутентифікацію та авторизацію
- [ ] Додати тести
- [ ] Оптимізувати запити до БД (load balancing)
- [ ] Додати логування помилок

## 📄 Ліцензія

ISC

## 👤 Автор

Aksel-141
